
&НаКлиенте
Процедура ПриОткрытии(Отказ)

	Если ЭтотОбъект.ПараметрыВыбора.Свойство("Подразделение") Тогда
		Объект.Подразделение = ЭтотОбъект.ПараметрыВыбора.Подразделение;
	КонецЕсли;
	
КонецПроцедуры
